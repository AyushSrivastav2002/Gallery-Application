A Gallery web application developed in React and styled in Tailwind CSS having the gallery of almost 200 images .

We can click for any image and can download that image.

Uses React Hooks(UseState Hook & UseEffect Hook)
Pagination. 
